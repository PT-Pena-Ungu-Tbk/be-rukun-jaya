{
  "openapi": "3.0.3",
  "info": {
    "title": "API Sistem Kasir Toko Rukun Jaya",
    "description": "Dokumentasi interaktif POS & Ekosistem Manajemen Inventaris untuk PT. Pena Ungu Tbk.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://localhost:5000/api/v1",
      "description": "Development Server Lokal"
    }
  ],
  "components": {
    "securitySchemes": {
      "BearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "description": "Masukkan token JWT"
      }
    }
  },
  "paths": {
    "/auth/login": {
      "post": {
        "summary": "Autentikasi Pengguna",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["email", "password"],
                "properties": {
                  "email": { "type": "string", "example": "owner@toko-rukunjaya.com" },
                  "password": { "type": "string", "example": "password_rahasia" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Login Berhasil" },
          "401": { "description": "Unauthorized" }
        }
      }
    },
    "/products": {
      "get": {
        "summary": "Ambil Daftar Produk",
        "security": [{ "BearerAuth": [] }],
        "parameters": [
          { "name": "search", "in": "query", "schema": { "type": "string" } },
          { "name": "low_stock", "in": "query", "schema": { "type": "boolean" } }
        ],
        "responses": {
          "200": { "description": "Daftar produk ditarik" }
        }
      },
      "post": {
        "summary": "Tambah Master Data Produk",
        "security": [{ "BearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "sku_code": { "type": "string", "example": "PIP-WV-AW12" },
                  "name": { "type": "string", "example": "Pipa Wavin AW 1/2 inch" },
                  "category_id": { "type": "string", "format": "uuid" },
                  "supplier_id": { "type": "string", "format": "uuid" },
                  "buy_price": { "type": "number", "example": 28000 },
                  "sell_price": { "type": "number", "example": 32000 },
                  "current_stock": { "type": "integer", "example": 120 },
                  "min_stock": { "type": "integer", "example": 50 },
                  "rack_location": { "type": "string", "example": "R-C3-12" }
                }
              }
            }
          }
        },
        "responses": { "201": { "description": "Produk berhasil ditambah" } }
      }
    },
    "/products/bulk-update": {
      "put": {
        "summary": "Bulk Update Stok Barang",
        "security": [{ "BearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "updates": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "id": { "type": "string", "format": "uuid" },
                        "new_stock": { "type": "integer" }
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "Update massal sukses" } }
      }
    },
    "/members/verify": {
      "get": {
        "summary": "Verifikasi Status Member VIP",
        "security": [{ "BearerAuth": [] }],
        "parameters": [
          { "name": "phone", "in": "query", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Member ditemukan" },
          "404": { "description": "Member tidak ditemukan" }
        }
      }
    },
    "/transactions/checkout": {
      "post": {
        "summary": "Finalisasi Transaksi Penjualan (POS)",
        "security": [{ "BearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "member_id": { "type": "string", "nullable": true },
                  "discount_type": { "type": "string", "enum": ["NOMINAL", "PERCENTAGE"] },
                  "discount_value": { "type": "number" },
                  "cash_paid": { "type": "number" },
                  "items": {
                    "type": "array",
                    "items": {
                      "type": "object",
                      "properties": {
                        "product_id": { "type": "string", "format": "uuid" },
                        "quantity": { "type": "integer" }
                      }
                    }
                  }
                }
              }
            }
          }
        },
        "responses": { "201": { "description": "Transaksi sukses" } }
      }
    },
    "/transactions/return": {
      "post": {
        "summary": "Retur Barang Cacat (Garansi Toko)",
        "security": [{ "BearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "transaction_id": { "type": "string", "format": "uuid" },
                  "product_id": { "type": "string", "format": "uuid" },
                  "quantity_returned": { "type": "integer" }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "Retur berhasil" } }
      }
    },
    "/reports/financial": {
      "get": {
        "summary": "Laporan Keuangan",
        "security": [{ "BearerAuth": [] }],
        "parameters": [
          { "name": "start_date", "in": "query", "required": true, "schema": { "type": "string", "example": "2026-06-01" } },
          { "name": "end_date", "in": "query", "required": true, "schema": { "type": "string", "example": "2026-06-30" } }
        ],
        "responses": { "200": { "description": "Data Laporan" } }
      }
    },
    "/audit-logs": {
      "get": {
        "summary": "Log Audit",
        "security": [{ "BearerAuth": [] }],
        "responses": { "200": { "description": "Daftar Audit Log" } }
      }
    }
  }
}